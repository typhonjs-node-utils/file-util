# Changelog
## 0.3.0 release
- Added `createReadable`, `createWritable`, and `isFileGzip` functions.

## 0.2.1 release
- Add `default` export conditions mirroring the `node` condition.

## 0.2.0 release
- Switch from `upath` to `pathe` for ESM support.

## 0.1.0 release
- Initial release
